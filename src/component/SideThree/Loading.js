import React from 'react'
import { CircularProgress } from '@material-ui/core'

const Loading = () => {
  return (
    <div>
        <div>Loading...</div>
        <CircularProgress />
    </div>
  )
}

export default Loading